# FAST-LIVO2 输出目录说明

本项目的 `Log/` 下存在三类**用途不同**的结果。它们不能只凭目录名都叫“地图”而混为一谈。

## 先记住这一条

FAST-LIVO2 的建图坐标参考默认是启动时建立的 SLAM 世界坐标（代码和证据文件中记为 `camera_init`）。它的原点、朝向由建图开始时的状态决定，**不是经纬度坐标，也不是自动对齐到 RTK 的 ENU 坐标**。

因此，RTK 经纬度若要和该坐标内的定位轨迹比较，必须有经过验证的
`WGS84 经纬度 -> 本地 ENU -> FAST-LIVO2 建图坐标` 变换；不能直接把经纬度数值当作 XYZ。

## 目录和用途

| 目录 | 产生方式 | 主要内容 | 是否是 FAST-LOCALIZATION 的三维先验地图 |
| --- | --- | --- | --- |
| `Log/pcd/` | 常规 `mapping_robotdog.launch` | 原始/降采样 PCD、稠密 RGB PCD、部分轨迹和优化结果 | 仅当同一次运行的关键帧和优化位姿完整保留时才可用；该旧默认目录容易被下一次运行混写，不应再作为长期地图仓库 |
| `Log/nav_runs/<map_id>/` | `mapping_robotdog_nav_map.launch` 的一次独立建图运行 | `keyframes/`、`loop_backend/`、`pcd/`、`dense_rgb_cache/` 等三维建图资产 | **是**。FAST-LOCALIZATION 的 `map_dir` 应指向此目录，例如 `Log/nav_runs/indoor_01` |
| `Log/nav_runs/<map_id>/nav_map_terrain_final/` | 离线 `fast_livo_nav_map_builder` | `map.pgm`、`map.yaml`、`map.bt`、地形/可通行性栅格 | 否。这是从同一套三维关键帧派生出的二维导航地图 |
| `Log/scene_evidence_<run_id>/` | 证据导出和 `scene_pipeline` 离线处理 | 逐帧 RGB 证据、几何/道路/对象提取、矢量结果、2.5D 渲染 | 否。它是面向道路与可视场景表达的后处理产品，不是定位的点云先验地图 |

## `indoor_01` 的真实角色

`Log/nav_runs/indoor_01` 同时是：

1. 一次独立的三维 SLAM 建图运行存档；
2. 二维导航栅格的输入；
3. 当前 FAST-LOCALIZATION 的三维先验地图输入。

其中 FAST-LOCALIZATION 实际读取的是：

```text
Log/nav_runs/indoor_01/
├── keyframes/metadata.yaml
├── keyframes/<关键帧>.pcd
└── loop_backend/optimized_keyframe_poses_imu.txt
```

它不读取：

```text
Log/nav_runs/indoor_01/nav_map_terrain_final/map.pgm
Log/nav_runs/indoor_01/nav_map_terrain_final/map.yaml
```

`nav_map_terrain_final` 只是使用相同的三维关键帧和优化位姿，向下生成二维占据/可通行性地图；它不会反过来改变三维关键帧或定位地图。

## `scene_evidence_20260814_01` 的真实角色

这个目录并非“把三维点云直接渲染成 2.5D”这么简单。它保存的是场景证据处理流水线的输出；其第 7 阶段才生成 2.5D 导航式渲染结果。证据文件元数据明确标注 `cloud_frame: camera_init`，并要求在需要全局回环结果时应用相应的回环校正。

它可用于场景理解、道路/边界/可视对象表达和展示，但不是 FAST-LOCALIZATION 的地图输入，也不能替代 RTK 真值。

## 以后建议的固定组织方式

从下一次建图开始，一个 `map_id` 只表示一张三维定位地图，并只在一个目录内保存其派生产品：

```text
Log/
└── nav_runs/
    └── <map_id>/
        ├── keyframes/                 # 三维定位地图的源数据
        ├── loop_backend/              # 回环优化位姿和全局优化 PCD
        ├── pcd/                       # 该 map_id 的三维地图成品
        ├── dense_rgb_cache/           # 可选：稠密 RGB 重建缓存
        ├── nav_map_terrain_final/     # 从本地图派生的二维导航图
        └── manifest.yaml               # 建图来源和坐标参考说明（建议新增）
```

而场景产品应使用不同的运行标识，保留在：

```text
Log/scene_evidence_<scene_run_id>/
```

## `manifest.yaml` 最少应记录什么

每张未来地图建议人工创建一个只读说明文件，至少包括：

```yaml
map_id: indoor_01
purpose: localization_prior_map
source_bag: <原始数据集路径或不可变标识>
source_capture_time: <采集时间>
coordinate_frame: camera_init
pose_reference: optimized_keyframe_poses_imu.txt
lidar_imu_extrinsic_source: config/robotdog.yaml
rtk_alignment: unverified  # 只有完成独立验证后才能写 verified
```

这能避免把“同一个地点的另一次采集”、“二维栅格”和“用于定位的三维先验图”误认为是同一份可直接比较的数据。

## 当前已验证定位时应使用的路径

```bash
roslaunch fast_localization localization_robotdog.launch \
  map_dir:=/root/catkin_ws/src/FAST-LIVO2/Log/nav_runs/indoor_01 \
  trajectory_dir:=/root/autodl-tmp/localization_results/<test_id> \
  rviz:=false
```

定位输出轨迹与 `indoor_01` 中由优化关键帧构建的三维点云处于同一 FAST-LIVO2 建图坐标参考下。它们可以直接在 CloudCompare 叠加；但要与 RTK 比较，仍须先验证 RTK 到此坐标参考的变换及天线杆臂。

## 当前可用的 RTK 可视化参考轨迹

随 2026-08-15 数据集提供的 `rtk_map_proxy.py` 给出了 `WGS84 -> ENU -> PCD map`
变换，也已提供 `UniRtkPvh`、`UniHeading`、`UniBestNav` 的完整消息定义。下列工具
直接解析组合消息 `/rtk_pvh`，严格使用代理脚本的质量门限，仅导出通过门限的蓝色
RTK 参考轨迹：

```bash
source /opt/ros/noetic/setup.bash

export DATASET=/root/autodl-tmp/Dataset/机器狗RTK数据_20260815
export SESSION=localization_20260815_175834
export OUT_DIR=/root/autodl-tmp/localization_results/$SESSION/rtk_reference

python3 /root/catkin_ws/src/FAST-LIVO2/scripts/export_robotdog_rtk_reference.py \
  "$DATASET/$SESSION/rosbag/rosbag_0.db3" \
  --output-dir "$OUT_DIR"
```

输出的 `rtk_reference_quality_pass_blue.pcd` 为蓝色参考轨迹，CSV 同时保留原始
经纬度、RTK 质量字段和转换后的地图 XYZ。质量门限为：定位状态正常、位置类型不低于
34、纬/经向标准差小于 `0.5 m`、高程标准差小于 `1.0 m`、差分龄期小于 `5 s`、航向类型
不低于 40、航向标准差小于 `5°`。

当前默认采用与该次数据快照同源的 `TZ=-4.92500000`。这是**可视化参考轨迹**：本轮
不计算 ATE/RMSE，也不以它报告绝对精度。
