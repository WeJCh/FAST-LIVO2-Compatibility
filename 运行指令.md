# FAST-LIVO2 运行指令

在使用任何 `Log/` 结果前，先阅读同目录的 [输出目录说明.md](输出目录说明.md)。特别是：
`Log/nav_runs/<map_id>` 是独立三维建图运行包；其下的 `nav_map_terrain_final/`
只是由该三维地图派生出的二维导航栅格。FAST-LOCALIZATION 使用前者的
`keyframes/` 和 `loop_backend/`，不读取二维 PGM 栅格。

### 代码同步后：重新编译

```
source /opt/ros/noetic/setup.bash
cd /root/catkin_ws
catkin_make -DCMAKE_BUILD_TYPE=RelWithDebInfo -DBUILD_LOOP_BACKEND=ON -j4
source /root/catkin_ws/devel/setup.bash
```

### 终端 1：启动 FAST-LIVO2

```
source /opt/ros/noetic/setup.bash
source /root/catkin_ws/devel/setup.bash
roslaunch \
  --sigint-timeout=1800 \
  --sigterm-timeout=120 \
  fast_livo mapping_robotdog.launch rviz:=false
```

### 终端 2：实时转换并发布机器狗数据

```
source /opt/ros/noetic/setup.bash
source /root/catkin_ws/devel/setup.bash
python3 /root/catkin_ws/src/FAST-LIVO2/scripts/play_ros2_robotdog_to_ros1.py /root/autodl-tmp/Dataset/session_20260722 --rate 1.0 --wait-subscribers
```

### 数据播放结束后：自动导出最终地图

在终端 1 按一次 `Ctrl+C`，不要再次中断，等待出现：

```
[dense-rgb] Automatic final RGB map export completed: ...
[dense-rgb] Intermediate cleanup complete.
```

最终保留文件：

```
Log/pcd/all_raw_points.pcd
Log/pcd/all_downsampled_points.pcd
Log/pcd/all_global_optimized_rgb_dense_full.pcd
Log/pcd/optimized_keyframe_poses_imu.txt
```

### FAST-LOCALIZATION：完整数据定位与轨迹输出

前提是已存在对应的三维先验地图。定位期间不要同时启动 FAST-LIVO2 的建图节点。
下面以第二段机器狗数据为例，红色定位轨迹和可选的 ROS1 结果包均保存到
`FAST-LOCALIZATION/Log/localization_results/full_175834` 对应的云服务器目录。

终端 1，启动定位节点：

```bash
source /opt/ros/noetic/setup.bash
source /root/catkin_ws/devel/setup.bash

unset OMP_NUM_THREADS

MAP_DIR=/root/catkin_ws/src/FAST-LIVO2/Log/nav_runs/indoor_01
OUT_DIR=/root/autodl-tmp/localization_results/full_175834

roslaunch fast_localization localization_robotdog.launch \
  rviz:=false \
  map_format:=fast_livo2 \
  map_dir:="$MAP_DIR" \
  trajectory_dir:="$OUT_DIR"
```

终端 2，若需要生成 `localization_result.bag`，再启动录制。该步骤对自动生成红色
CSV/PCD 不是必需的：

```bash
source /opt/ros/noetic/setup.bash
source /root/catkin_ws/devel/setup.bash

OUT_DIR=/root/autodl-tmp/localization_results/full_175834
mkdir -p "$OUT_DIR"

rosbag record --lz4 -O "$OUT_DIR/localization_result" \
  /Odometry /tf /tf_static
```

终端 3，播放数据。应在终端 1 显示地图加载成功后再执行：

```bash
source /opt/ros/noetic/setup.bash
source /root/catkin_ws/devel/setup.bash

BAG_DIR='/root/autodl-tmp/Dataset/机器狗RTK数据_20260815/localization_20260815_175834/rosbag'

python3 /root/catkin_ws/src/FAST-LIVO2/scripts/play_ros2_robotdog_to_ros1.py \
  "$BAG_DIR" \
  --rate 1.0 \
  --skip-images \
  --wait-subscribers
```

播放结束后，先在终端 2 按 `Ctrl+C` 停止可选录包；再在终端 1 按一次 `Ctrl+C`。
定位节点在全局初始化成功后才自动开始写入红色轨迹，并在正常退出时保存：

```text
localization_trajectory_<日期>_<时间>_pid<进程号>.csv
localization_trajectory_<日期>_<时间>_pid<进程号>.pcd
```

### RTK：导出未筛选参考轨迹

此步骤直接离线解析原始 ROS2 bag 的 `/rtk_pvh`，不需要 ROS2 环境。它生成误差
统计所用的未筛选 RTK CSV 和蓝色 PCD：

```bash
export DATASET=/root/autodl-tmp/Dataset/机器狗RTK数据_20260815
export SESSION=localization_20260815_175834
export OUT_DIR=/root/autodl-tmp/localization_results/$SESSION/rtk_reference_raw_pvh

mkdir -p "$OUT_DIR"

python3 /root/catkin_ws/src/FAST-LIVO2/scripts/export_robotdog_rtk_reference.py "$DATASET/$SESSION/rosbag/rosbag_0.db3" --output-dir "$OUT_DIR" --mode raw
```

主要输出为：

```text
rtk_reference_trajectory.csv       # 未筛选 RTK；含 timestamp_sec、map_x、map_y、map_z
rtk_reference_trajectory_blue.pcd  # 蓝色 RTK 轨迹，供 CloudCompare 叠加
rtk_reference_trajectory_summary.txt
```

质量筛选版本仍写入同一原始解析阶段目录 `rtk_reference_raw_pvh/`。需要生成时，
仅将上述命令末尾的 `--mode raw` 改为 `--mode quality`；它会额外生成
`rtk_reference_quality_pass.csv` 与对应蓝色 PCD，不会覆盖未筛选文件。

### RTK：天线轨迹补偿到 IMU 参考点

FAST-LOCALIZATION 自动导出的红色轨迹记录的是 IMU 状态位置，而原始 RTK CSV
记录的是天线位置。以下步骤使用厂商杆臂 `天线 → imu_front = [+0.40000, 0] m`，
按每帧 `map_yaw_deg` 旋转后补偿 RTK 的 XY。它不修改原始 RTK 文件，也不处理 Z。

```bash
export RTK_RAW_DIR=/root/autodl-tmp/localization_results/localization_20260815_175834/rtk_reference_raw_pvh
export RTK_IMU_DIR=/root/autodl-tmp/localization_results/localization_20260815_175834/rtk_reference_imu_compensated
mkdir -p "$RTK_IMU_DIR"
python3 /root/catkin_ws/src/FAST-LIVO2/scripts/compensate_rtk_lever_arm.py --input-csv "$RTK_RAW_DIR/rtk_reference_trajectory.csv" --output-dir "$RTK_IMU_DIR"
```

输出：

```text
rtk_reference_imu_trajectory.csv       # map_x/map_y 已补偿到 IMU；保留 antenna_map_x/y 供审计
rtk_reference_imu_trajectory_blue.pcd  # 水平补偿后的蓝色轨迹
rtk_reference_imu_trajectory_summary.txt
```

### 质量筛选 RTK 的对应改动

质量筛选 RTK 用于生成更可信的**补充对照指标**，但会使轨迹断续；它不应覆盖
未筛选 RTK 的连续性评估结果。所有质量筛选产物仍按阶段写入既有目录：原始解析
结果写在 `rtk_reference_raw_pvh/`，IMU 补偿结果写在 `rtk_reference_imu_compensated/`。

第一步：导出质量筛选后的天线轨迹。该命令不会覆盖同目录的未筛选文件：

```bash
export DATASET=/root/autodl-tmp/Dataset/机器狗RTK数据_20260815
export SESSION=localization_20260815_175834
export OUT_DIR=/root/autodl-tmp/localization_results/$SESSION/rtk_reference_raw_pvh
mkdir -p "$OUT_DIR"
python3 /root/catkin_ws/src/FAST-LIVO2/scripts/export_robotdog_rtk_reference.py "$DATASET/$SESSION/rosbag/rosbag_0.db3" --output-dir "$OUT_DIR" --mode quality
```

第二步：将质量筛选天线轨迹补偿到 IMU 参考点：

```bash
export RTK_RAW_DIR=/root/autodl-tmp/localization_results/localization_20260815_175834/rtk_reference_raw_pvh
export RTK_IMU_DIR=/root/autodl-tmp/localization_results/localization_20260815_175834/rtk_reference_imu_compensated
mkdir -p "$RTK_IMU_DIR"
python3 /root/catkin_ws/src/FAST-LIVO2/scripts/compensate_rtk_lever_arm.py --input-csv "$RTK_RAW_DIR/rtk_reference_quality_pass.csv" --output-dir "$RTK_IMU_DIR" --prefix rtk_reference_quality_imu_trajectory --input-quality-gated
```

第三步：生成质量筛选误差指标。该命令与未筛选结果共用 `EVAL_DIR`，但质量筛选的
四份 CSV 使用 `quality_imu_xy_error_` 前缀，不会覆盖 `xy_error_*.csv`：

```bash
export LOC_CSV=/root/autodl-tmp/localization_results/full_175834/localization_trajectory_20260824_124058_pid4672.csv
export RTK_CSV=/root/autodl-tmp/localization_results/localization_20260815_175834/rtk_reference_imu_compensated/rtk_reference_quality_imu_trajectory.csv
export EVAL_DIR=/root/autodl-tmp/localization_results/full_175834/xy_rtk_evaluation_imu_lever_arm
python3 /root/catkin_ws/src/FAST-LIVO2/scripts/evaluate_localization_xy.py --localization-csv "$LOC_CSV" --rtk-csv "$RTK_CSV" --output-dir "$EVAL_DIR" --rtk-reference-label "质量筛选 RTK" --output-prefix quality_imu_xy_error
```

质量筛选会造成时间空洞，`paired_sample_count` 将小于未筛选评估的 4850；只统计
真正具有合格 RTK 参考的时刻，这是预期行为，不应把缺失时段当成定位失败。

### 已完成定位结果的 XY 误差统计（IMU 杆臂补偿后的未筛选 RTK）

此步骤不启动 ROS、不重新解析原始 rosbag。它直接比较已经生成的红色定位
CSV 与已补偿至 IMU 的未筛选 RTK CSV 的共同时间段；只计算 XY，忽略 Z。相邻 RTK 采样在
0.5 秒内时会进行时间插值；这不是质量筛选。全局初始化完成前
红色轨迹尚不存在的前段会单独记录为覆盖缺失，但不会被误算成定位误差。

以下示例对应第二段完整数据。请按实际红色轨迹文件名调整 `LOC_CSV`：

```bash
source /opt/ros/noetic/setup.bash

export LOC_CSV=/root/autodl-tmp/localization_results/full_175834/localization_trajectory_20260824_124058_pid4672.csv
export RTK_CSV=/root/autodl-tmp/localization_results/localization_20260815_175834/rtk_reference_imu_compensated/rtk_reference_imu_trajectory.csv
export EVAL_DIR=/root/autodl-tmp/localization_results/full_175834/xy_rtk_evaluation_imu_lever_arm

python3 /root/catkin_ws/src/FAST-LIVO2/scripts/evaluate_localization_xy.py \
  --localization-csv "$LOC_CSV" \
  --rtk-csv "$RTK_CSV" \
  --output-dir "$EVAL_DIR"
```

结果目录包含：

```text
xy_error_summary.csv            # 整体：XY MAE/RMSE/P95、X/Y 分量统计、覆盖信息
xy_error_pairs.csv              # 每个成功时间匹配的红/蓝坐标及 XY 误差
xy_error_time_segments.csv      # 每 30 秒分段统计
xy_error_spatial_grid.csv       # 20 m × 20 m 空间网格统计
README.md                       # 指标口径与限制
```

这里的 MAE 是每时刻 XY 欧氏距离的平均偏差；RMSE 对突发大误差更敏感；
P95 表示 95% 的已匹配时刻不超过的误差。`x_signed_mean_error`、
`y_signed_mean_error` 为带符号偏差（定位减 RTK）；`x/y_mae`、`x/y_rmse`、
`x/y_p95` 分别统计 `|dx|`、`|dy|`，用于诊断误差主要集中在哪个地图坐标轴方向。
由于 RTK 未做质量筛选，即使已施加 GNSS 天线到 IMU 的水平杆臂修正，
这些指标仍应称为“相对未筛选 RTK 的 XY
偏差”，不能作为严格的绝对真值 ATE 对外报告。

### 静态三维地图转二维导航地图

首次同步二维地图工具代码后，使用以下配置重新编译：

```bash
source /opt/ros/noetic/setup.bash
cd /root/catkin_ws
catkin_make --pkg fast_livo -j4 --cmake-args \
  -DCMAKE_BUILD_TYPE=RelWithDebInfo \
  -DBUILD_LOOP_BACKEND=ON \
  -DBUILD_NAV_MAP_BUILDER=ON \
  -DBUILD_DYNAMIC_REMOVAL=OFF
source devel/setup.bash
```

离线二维图生成工具的当前参数均已有默认值。每次选择已有关键帧的 `RUN_ID`，并使用一个新的输出目录名：

```bash
source /opt/ros/noetic/setup.bash
source /root/catkin_ws/devel/setup.bash

export RUN_ID=indoor_01
export RUN_DIR=/root/catkin_ws/src/FAST-LIVO2/Log/nav_runs/$RUN_ID

rosrun fast_livo fast_livo_nav_map_builder \
  --keyframes $RUN_DIR/keyframes \
  --poses $RUN_DIR/loop_backend/optimized_keyframe_poses_imu.txt \
  --output $RUN_DIR/nav_map_terrain_final
```

当前默认值已包含：`0.10 m` 地图分辨率、`1.25 m` 主尺度地面拟合、`2.00 m` 多尺度后备拟合、`0.05--0.65 m` 障碍高度带、两帧障碍/自由射线确认，以及自适应弱障碍门限。

生成后检查：

```bash
test -s $RUN_DIR/nav_map_terrain_final/map.pgm
test -s $RUN_DIR/nav_map_terrain_final/map.yaml
test -s $RUN_DIR/nav_map_terrain_final/fallback_free_support.pgm
cat $RUN_DIR/nav_map_terrain_final/map_metadata.yaml
```

查看当前编译版本的完整参数与默认值：

```bash
rosrun fast_livo fast_livo_nav_map_builder --help
```
