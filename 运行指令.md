### 代码同步后：重新编译

```
source /opt/ros/noetic/setup.bash
cd /root/catkin_ws
catkin_make -DCMAKE_BUILD_TYPE=RelWithDebInfo -DBUILD_LOOP_OPTIMIZER=ON -j4
source /root/catkin_ws/devel/setup.bash
```

### 终端 1：启动 FAST-LIVO2

```
source /opt/ros/noetic/setup.bash
source /root/catkin_ws/devel/setup.bash
roslaunch \
  --sigint-timeout=1800 \
  --sigterm-timeout=120 \
  fast_livo mapping_robotdog.launch rviz:=false
```

### 终端 2：实时转换并发布机器狗数据

```
source /opt/ros/noetic/setup.bash
source /root/catkin_ws/devel/setup.bash
python3 /root/catkin_ws/src/FAST-LIVO2/scripts/play_ros2_robotdog_to_ros1.py /root/autodl-tmp/Dataset/session_20260722 --rate 1.0 --wait-subscribers
```

### 数据播放结束后：自动导出最终地图

在终端 1 按一次 `Ctrl+C`，不要再次中断，等待出现：

```
[dense-rgb] Automatic final RGB map export completed: ...
[dense-rgb] C2 cleanup complete.
```

最终保留文件：

```
Log/pcd/all_raw_points.pcd
Log/pcd/all_downsampled_points.pcd
Log/pcd/all_global_optimized_rgb_dense_full.pcd
Log/pcd/optimized_keyframe_poses_imu.txt
```

### 自动导出失败时：离线恢复稠密彩色优化地图

仅在 RGB 缓存和关键帧目录仍被保留时使用：

```
source /opt/ros/noetic/setup.bash
source /root/catkin_ws/devel/setup.bash

rosrun fast_livo fast_livo_rgb_reproject \
  --cache_dir /root/catkin_ws/src/FAST-LIVO2/Log/dense_rgb_cache_robotdog_c2 \
  --raw_keyframe_poses /root/catkin_ws/src/FAST-LIVO2/Log/pcd/keyframes_robotdog_c2/keyframe_poses_imu.txt \
  --optimized_keyframe_poses /root/catkin_ws/src/FAST-LIVO2/Log/loop_online_final_robotdog_c2/optimized_keyframe_poses_imu.txt \
  --output /root/catkin_ws/src/FAST-LIVO2/Log/pcd/all_global_optimized_rgb_dense_full.pcd \
  --voxel_leaf 0
```
