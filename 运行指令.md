### 代码同步后：重新编译

```
source /opt/ros/noetic/setup.bash
cd /root/catkin_ws
catkin_make -DCMAKE_BUILD_TYPE=RelWithDebInfo -DBUILD_LOOP_BACKEND=ON -j4
source /root/catkin_ws/devel/setup.bash
```

### 终端 1：启动 FAST-LIVO2

```
source /opt/ros/noetic/setup.bash
source /root/catkin_ws/devel/setup.bash
roslaunch \
  --sigint-timeout=1800 \
  --sigterm-timeout=120 \
  fast_livo mapping_robotdog.launch rviz:=false
```

### 终端 2：实时转换并发布机器狗数据

```
source /opt/ros/noetic/setup.bash
source /root/catkin_ws/devel/setup.bash
python3 /root/catkin_ws/src/FAST-LIVO2/scripts/play_ros2_robotdog_to_ros1.py /root/autodl-tmp/Dataset/session_20260722 --rate 1.0 --wait-subscribers
```

### 数据播放结束后：自动导出最终地图

在终端 1 按一次 `Ctrl+C`，不要再次中断，等待出现：

```
[dense-rgb] Automatic final RGB map export completed: ...
[dense-rgb] Intermediate cleanup complete.
```

最终保留文件：

```
Log/pcd/all_raw_points.pcd
Log/pcd/all_downsampled_points.pcd
Log/pcd/all_global_optimized_rgb_dense_full.pcd
Log/pcd/optimized_keyframe_poses_imu.txt
```

### 静态三维地图转二维导航地图

首次同步二维地图工具代码后，使用以下配置重新编译：

```bash
source /opt/ros/noetic/setup.bash
cd /root/catkin_ws
catkin_make --pkg fast_livo -j4 --cmake-args \
  -DCMAKE_BUILD_TYPE=RelWithDebInfo \
  -DBUILD_LOOP_BACKEND=ON \
  -DBUILD_NAV_MAP_BUILDER=ON \
  -DBUILD_DYNAMIC_REMOVAL=OFF
source devel/setup.bash
```

离线二维图生成工具的当前参数均已有默认值。每次选择已有关键帧的 `RUN_ID`，并使用一个新的输出目录名：

```bash
source /opt/ros/noetic/setup.bash
source /root/catkin_ws/devel/setup.bash

export RUN_ID=indoor_01
export RUN_DIR=/root/catkin_ws/src/FAST-LIVO2/Log/nav_runs/$RUN_ID

rosrun fast_livo fast_livo_nav_map_builder \
  --keyframes $RUN_DIR/keyframes \
  --poses $RUN_DIR/loop_backend/optimized_keyframe_poses_imu.txt \
  --output $RUN_DIR/nav_map_terrain_final
```

当前默认值已包含：`0.10 m` 地图分辨率、`1.25 m` 主尺度地面拟合、`2.00 m` 多尺度后备拟合、`0.05--0.65 m` 障碍高度带、两帧障碍/自由射线确认，以及自适应弱障碍门限。

生成后检查：

```bash
test -s $RUN_DIR/nav_map_terrain_final/map.pgm
test -s $RUN_DIR/nav_map_terrain_final/map.yaml
test -s $RUN_DIR/nav_map_terrain_final/fallback_free_support.pgm
cat $RUN_DIR/nav_map_terrain_final/map_metadata.yaml
```

查看当前编译版本的完整参数与默认值：

```bash
rosrun fast_livo fast_livo_nav_map_builder --help
```
